# PrintQue Frontend

React frontend for PrintQue. See the [repository root README](../README.md) for full setup and development instructions.

**From this directory:**

- `npm install` — Install dependencies
- `npm run dev` — Start development server (port 3000)
- `npm run build` — Production build
- `npm run test` — Run tests
